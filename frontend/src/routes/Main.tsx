import React, {FC} from 'react';
import { Box, Button } from '@chakra-ui/react';

const Main: FC = () => {
    return (
        <Box>
            <Button colorScheme={'blue'}>Main</Button>
        </Box>
    )
}

export default Main;